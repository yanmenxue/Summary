# 核心通信原语详解

## 一对多 / 多对一：基础操作

这类操作通常涉及一个"根"节点（Root Rank）和所有其他节点。

### Broadcast (广播)：一对多

根节点将一份完全相同的数据发送给所有其他节点。

**典型用途**：模型参数初始化，确保所有GPU上的初始模型权重一致。

---

### Scatter (散射)：一对多

根节点将数据切分成不同的块，每块分别发送给对应的节点。

**典型用途**：将一批数据分散到不同的GPU上进行处理。

---

### Gather (聚合)：多对一

所有节点将各自的数据发送给根节点，根节点再将它们拼接成一个完整的数据。它是Scatter的逆操作。

---

### Reduce (规约)：多对一

所有节点将数据发送给根节点，根节点对这些数据执行一个规约操作（如求和、求最大值、求最小值等）。

**典型用途**：在参数服务器架构中，从多个Worker节点收集梯度更新并聚合到Master节点。

---

## 多对多：数据并行与模型并行的核心

这些操作是所有节点都参与的集体操作，是构建数据并行和模型并行的基础。

### AllReduce (全规约)：多对多

可以理解为 Reduce + Broadcast 的组合。所有节点先执行规约操作（如梯度求和），然后将最终结果同步给每一个节点，让每个节点都拥有最终结果的全量副本。

**典型用途**：数据并行训练的核心。在每个训练步结束后，不同GPU上的梯度通过一次AllReduce进行同步和平均，确保所有模型副本的参数保持一致。

---

### AllGather (全聚合)：多对多

可以理解为 Gather + Broadcast 的组合。每个节点都将自己的数据发送出去，同时接收所有其他节点的数据，并将它们拼接起来，最终每个节点都拥有所有数据的完整集合。

**典型用途**：在模型并行（张量并行）中，需要某层的全部权重才能计算时使用。例如，Megatron-LM在张量并行前向计算中，会使用AllGather来聚合被切分到不同GPU上的参数。

---

### ReduceScatter (规约散射)：多对多

它是 Reduce + Scatter 的组合。所有节点先执行规约操作，但结果不是发给所有人，而是按块切分，每个节点只收到属于自己的那一块。

**典型用途**：常与AllGather成对出现，作为其逆向操作。在DeepSpeed的ZeRO优化器中，反向传播后会使用ReduceScatter将梯度分片规约并分发到各节点。

---

### All-to-All (全交换)：多对多

这是一种更灵活的交换模式。每个节点都会向所有其他节点（包括自己）发送不同的数据块，并从所有节点接收不同的数据块。可以理解为对整个数据矩阵进行了"转置"操作。

**典型用途**：用于一些特殊的并行模式，如序列并行（DeepSpeed-Ulysses）和专家并行（MoE），以高效地在不同设备间重新分配数据。
